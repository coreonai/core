# F2 — minimal Domain impl

1. Domain that scores Correct only when completion is exactly `ok`.
2. Toy domain whose charset includes digits 0-9.
3. Domain that rejects empty completions.
4. Always-incorrect domain (for negative control).
5. (Korean) 완성이 `ok`일 때만 통과하는 Domain을 구현해.

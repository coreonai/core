# F1 — Tool stub + registry dispatch

Phrasing variants (same contract):
1. Implement tool `echo` that returns its args unchanged.
2. Add a Tool named `ping` that always returns `pong`.
3. Register `echo` in a ToolRegistry and dispatch `(echo hello)`.
4. (Korean) `echo` 툴을 만들고 인자를 그대로 돌려줘.
5. Tool whose `name()` is `upper` and returns args in ASCII uppercase.
